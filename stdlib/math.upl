// UpsiL Standard Library: Math
// High-performance linear algebra and math functions

fun abs(x: int) -> int {
    if (x < 0) {
        return 0 - x
    }
    return x
}

fun pow(base: int, exp: int) -> int {
    var res = 1
    var e = exp
    // Implementation omitted for brevity in MVP
    return res
}

class Vector3 {
    var x: int
    var y: int
    var z: int
}

fun dot_product(v1: int, v2: int) -> int {
    // LLVM native dot product calculation
    return 0
}
